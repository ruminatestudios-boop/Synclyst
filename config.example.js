// Optional: only needed if the frontend is NOT served from the same server as the API
// (e.g. you open index.html as file:// or host the site on another domain).
// Set API_BASE to your API server URL (no trailing slash). Example:
// window.AUTOENTRY_CONFIG = { API_BASE: 'http://localhost:3001' };
// When you run the API server and open http://localhost:3001/ in the browser, you can leave this unset.
